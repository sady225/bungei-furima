# 町の案内所・デジタル職員室 安全・公開ルール

対象：`index.html`、`ai-editor/`、今後追加するAI案内機能と公開コンテンツ

## 1. ブラウザ上の実装ルール

- 可変データの表示に `innerHTML`、`outerHTML`、`insertAdjacentHTML` を使わない。
- `textContent`、`createElement`、`appendChild`、`replaceChildren` を使う。
- 外部埋め込みは初期表示で読み込まない。利用者のクリック後に、固定された許可先だけを読み込む。
- 文字列からスクリプト、イベント属性、任意URLを生成しない。
- 依存ライブラリを追加する場合は、必要性、配信元、更新方法をレビューする。

## 2. 外部通信とCSP

`ai-editor/` は Phase 1〜2 では外部通信を行わない。CSPの `connect-src` と `frame-src` は `none` とする。音声案内は公開成果物に含めた同一オリジンの固定音声だけを利用し、`media-src` は `self` に限定する。

ずんだもん音声を掲載する場合は、利用箇所の近くに `VOICEVOX:ずんだもん` と表示する。音声は自動再生せず、利用者の明示操作後だけ再生する。

トップページで許可する外部通信先は次に限定する。

- Instagram埋め込み：`https://www.instagram.com`。利用者が「表示する」を押した後だけ接続する。
- 既存の出展作品画像：`https://pbs.twimg.com`。画像要素には `referrerPolicy="no-referrer"` を指定する。

本番ではHTMLのCSPメタタグに加え、Cloudflare側で次のHTTPレスポンスヘッダーを設定する。

- `Content-Security-Policy`：HTML内ポリシーと同等以上
- `Permissions-Policy: camera=(), microphone=(), geolocation=()`
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: no-referrer`
- `Cross-Origin-Opener-Policy: same-origin`

## 3. AI接続の境界

- Phase 1〜2ではAI APIへ接続しない。
- APIキーをHTML、JavaScript、公開リポジトリ、ビルド成果物へ置かない。
- AI接続時はサーバー側のエンドポイントを経由し、許可したAPIだけへ通信する。
- ブラウザからAI事業者へ直接送信しない。
- 入力長、レート、許可するデータ形式、タイムアウト、出力形式をサーバー側で制限する。
- 個別支援、緊急性、安全、学校連携、撮影、金銭、契約、法的判断は人間へ引き継ぐ。

## 4. ログと個人情報

- 初期案内では自由入力を設けず、選択した立場と閲覧先だけで案内する。
- 会話ログ、氏名、学校名、住所、連絡先、診断名、不登校理由、家庭事情を原則保存しない。
- ブラウザの `localStorage` / `sessionStorage` に相談内容を保存しない。
- 保存が必要な機能を追加する場合は、利用目的、項目、保存期間、閲覧権限、削除方法を先に文書化する。
- 緊急相談や個別相談を公開画面へ入力させず、人間の窓口へ案内する。

## 5. 画像・作品の公開許可

公開前に、事務局が画像単位で次を確認する。許可記録そのものは公開リポジトリへ置かない。

1. 権利者または出展者から、掲載ページ・目的・期間について許可を得たか。
2. 子どもが写る場合、保護者の同意に加えて本人の意思を確認したか。
3. 作品に第三者の著作物、氏名、学校名、名札、住所、車両番号などが写っていないか。
4. 顔を出す必要がなければ、作品中心、手元、後ろ姿、顔が分からない構図を優先したか。
5. 位置情報などのEXIFを削除したか。
6. 公開期限または再確認日、撤回時の連絡先、削除担当者を記録したか。

許可状態は内部台帳で `未確認 / 許可済み / 条件付き / 撤回 / 期限切れ` のいずれかとして管理する。`許可済み` または公開条件を満たす `条件付き` 以外は公開しない。

既存の `photo/` 内の公開画像についても、過去に公開済みであることを許可済みの根拠とはせず、同じルールで順次確認する。

## 6. やさしさの足あと

- 個人名、学校名、顔写真を使わず、出来事を匿名・集計で示す。
- 性格、優しさ、発達、社会性を評価・採点しない。
- 少人数の組合せから本人が推測できる記述を避ける。
- 本人が望まない出来事や、個別相談に関わる内容は掲載しない。

## 7. 公開前チェック

- `rg -n 'innerHTML|outerHTML|insertAdjacentHTML|document.write|eval\\('` が公開コードで0件。
- 初期表示時にInstagramのスクリプトやiframeが存在しない。
- APIキー形式、秘密情報、個人情報がGit管理対象にない。
- CSP違反、コンソールエラー、リンク切れがない。
- キーボード操作、フォーカス表示、読み上げ用ラベル、スマートフォン表示を確認する。
- `bash scripts/build-public-site.sh` が成功し、想定外のファイルが `_site/` に入っていない。

自動確認は `bash scripts/check-site-safety.sh` で実行する。
