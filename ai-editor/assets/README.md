# オンライン事務局の背景画像

このフォルダに、背景画像を次の名前で配置してください。

```text
office-background.png
```

元ファイルが `/mnt/data/image.png` として利用できる環境では、次の対応を行います。

```text
/mnt/data/image.png -> ai-editor/assets/office-background.png
```

画像が未配置でも、ページは予備背景色で読めるようにしてあります。
